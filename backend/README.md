Backend instructions:
- Copy .env.example to .env and edit DATABASE_URL if needed.
- To run locally with docker-compose: from backend folder run `docker-compose up --build`
- Backend listens on port 4000
